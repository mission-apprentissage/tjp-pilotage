const NAV_HEIGHT = 52;
const OFFSET = 16;

export const getStickyNavHeightWithOffset = () => {
  return `${NAV_HEIGHT + OFFSET}px`;
};

export const getStickyNavHeight = () => {
  return `${NAV_HEIGHT}px`;
};
